import {
  useAxiosPost,
  IAxiosResponse,
  RequestBodyWrapper,
} from '@/shared/libs';

const REGISTER_SOURCE_GROUP = 'register-source-group';

// export function useRegisterSourceGroup(REGISTER) {}
